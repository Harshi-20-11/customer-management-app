// db.js

const { Pool } = require('pg');

module.exports = {
    user: 'your_username',
    host: 'your_host',
    database: 'your_database_name',
    password: 'your_password',
    port: your_port,
  };
  

module.exports = pool;
